# Values diff for AWS Marketplace.
# This file contains only the differences from the base values.yaml for the AWS Marketplace environment.
# These values take precedence over the base values.yaml when merged at release time.

global:
  platform: "aws"
