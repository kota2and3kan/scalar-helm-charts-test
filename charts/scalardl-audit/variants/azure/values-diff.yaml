# Values diff for Azure Marketplace.
# This file contains only the differences from the base values.yaml for the Azure Marketplace environment.
# These values take precedence over the base values.yaml when merged at release time.
