# Values diff for Red Hat OpenShift.
# This file contains only the differences from the base values.yaml for the Red Hat OpenShift environment.
# These values take precedence over the base values.yaml when merged at release time.
